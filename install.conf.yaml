- defaults:
    link:
      relink: true

- clean: ['~']

# - link:
#     ~/.tmux.conf: tmux.conf
#     ~/.tmux.conf: tmux.conf
#     ~/.vim: vim
#     ~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/.vim/undo-history
    - ~/hey/you/guys

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]