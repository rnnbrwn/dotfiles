- defaults:
    link:
      relink: true

- clean: ['~']

- link:
      ~/.zshrc:
      ~/.gitconfig:
      ~/.vscode:
      ~/Library/Application Support/Code/User:

- create:
    - ~/Work
    - ~/.vim/undo-history

# - shell:
#  - [git submodule update --init --recursive, Installing submodules]